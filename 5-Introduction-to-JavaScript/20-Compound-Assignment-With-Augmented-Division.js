/* I leatrned about augmented division, which uses the quotient operator after
an equals sign to divide the variable on the left by the number on the right of
the compound operator. It looks like a "not impressed" meh face backwards /= */

var a = 48;
var b = 108;
var c = 33;

// Only modify code below this line

a /= 12;
b /= 4;
c /= 11;
