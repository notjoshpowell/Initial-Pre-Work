/* I learned that strings are immutable, and therfore cannot be changed from within
using bracket notation or another technique (discussed at this point). strings
are redefined by calling the string without var and the new string content to the
right of the = sign. */

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
