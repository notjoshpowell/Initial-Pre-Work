/* I learned that Javascipt variables have initialized values of undefined
 when first declared, causing a NaN result (non a number). If the variable is a string
 left undefined, the result will be "undefined" (a string). */

 // Initialize these three variables
var a = 5;
var b = 10;
var c = "I am a";

// Do not change code below this line

a = a + 1;
b = b + 5;
c = c + " String!";
